# MySQL 配置
# MySQL 镜像（会根据系统架构自动选择，也可手动指定）
# x86: 100zz/test-mysql:8.0.20
# ARM: 100zz/test-mysql:8.0.39-arm64v8
MYSQL_IMAGE=100zz/test-mysql:8.0.20
MYSQL_ROOT_PASSWORD=Test@123456
MYSQL_DATABASE=bug_management
MYSQL_USER=buguser
MYSQL_PASSWORD=Test@123456
MYSQL_PORT=33306

# 镜像标签（手动指定，默认为 latest）
# 例如：IMAGE_TAG=1.0.0 或 IMAGE_TAG=v1.2.3
# 注意：容器名称会自动添加时间戳后缀（格式：-年月日-时分秒）
IMAGE_TAG=latest

# 后端服务配置
BACKEND_PORT=43211

# 文件上传目录配置
# Docker 部署时，文件会保存到宿主机的这个目录，方便直接查看和备份
# 目录结构：
#   /opt/bug-uploads/
#     ├── local/{名称}/   # 本地上传的文件
#     └── flow/{名称}/    # 流程导出的文件
UPLOAD_HOST_DIR=/opt/bug-uploads

# 缺陷图片目录配置
# Docker 部署时，缺陷截图保存到宿主机的这个目录
# 目录结构：
#   /opt/bug-images/
#     ├── BUG-001/        # 以缺陷编号为文件夹名称
#     │   ├── a1b2c3d4.png
#     │   └── x9y8z7w6.jpg
#     └── ...
BUG_IMAGE_DIR=/opt/bug-images

# 代码扫描目录配置
# 宿主机上存放待扫描代码的目录，会映射到容器内的 /opt/code
# 新增 Sonar 扫描任务时，路径填写 /opt/code/项目名 即可
# 例如：/opt/code/saas 对应宿主机的 /opt/code/saas
CODE_SCAN_DIR=/opt/code

# Maven 目录配置（Java 项目扫描需要）
# 映射宿主机的 Maven 安装目录到容器内
MAVEN_HOME=/opt/common/apache-maven-3.9.6

# Java 目录配置（Maven 运行需要）
# 映射宿主机的 JDK 安装目录到容器内
JAVA_HOME=/opt/common/jdk-21.0.9

# 代码扫描：使用外部 SonarQube 服务（不部署 Sonar 容器，扫描页面调用该地址）
SONAR_URL=http://your-sonar-server:9000
SONAR_TOKEN=

# 前端服务配置
FRONTEND_PORT=11234

# npm/yarn registry 配置（如果容器内无法访问外网，需要配置内网 registry）
# 使用国内镜像源（默认）
# 如果需要使用内网 registry，可以修改为：NPM_REGISTRY=https://your-internal-registry.com
NPM_REGISTRY=https://registry.npmmirror.com
YARN_REGISTRY=https://registry.npmmirror.com

